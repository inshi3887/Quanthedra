[pytest]
timeout = 120
markers =
    integration: optional live exchange API smoke tests (requires your own testnet keys in env)
    stress: long-running synthetic market stress tests
    release_gate_db: P1 release-gate tests that require a disposable PostgreSQL (TEST_DATABASE_URL)
